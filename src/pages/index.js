import { SEO, Intro, Services, Blog, Contact } from '../components';

const Home = () => {
	return (
		<div>
			<SEO />
			<Intro />
			<Services />
			<Blog />
			<Contact />
		</div>
	);
};

export default Home;
